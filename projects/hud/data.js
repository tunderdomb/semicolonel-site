module.exports = function( project ){
  project
    .setName("Hud")
    .setType("browser")
    .describe("Everyone writes a DOM manip tool at some point. This is mine.")
    .addLink("github", "https://github.com/tunderdomb/hud", true)
    .tag("browser")
    .tag("DOM")
}