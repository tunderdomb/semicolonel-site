module.exports = function( project ){
  project
    .setName("Shapely")
    .setType("node")
    .describe("A slim data model library for node and the browser")
    .addLink("github", "https://github.com/tunderdomb/shapely", true)
    .tag("node")
    .tag("browser")
    .tag("data model")
}